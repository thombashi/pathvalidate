Filename validation
----------------------------

The :py:func:`.validate_filename()` function will raise ``ValueError`` if
the filename includes invalid character(s) for a filename.

.. include:: validate_code.txt
